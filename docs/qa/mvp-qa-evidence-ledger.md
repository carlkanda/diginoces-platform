# MVP QA Evidence Ledger

Traceability: GitHub issue `#31`; sprint plan `docs/planning/sprint-15-plan.md`; release backlog `EPIC-RELEASE`, especially `FEAT-REL-001`, `FEAT-REL-002`, `FEAT-REL-005`, and `FEAT-REL-010`; source scenarios `docs/qa/mvp-manual-qa-scenarios.md`; artifact-store rules `docs/setup/qa-artifact-store.md`.

## Purpose

This ledger is the production sign-off tracker for QA-001 through QA-036. It records only opaque evidence references, statuses, owners, and classifications. Real screenshots, exports, logs, credential references, guest data, client data, and artifact-store URLs must remain outside git in the encrypted QA artifact store or secure release runbook.

Production launch remains `no_go` until every scenario row is either:

- `pass` with an external evidence ID/reference;
- `fail` with a linked blocker issue and accepted classification;
- `waived` with owner approval, remediation date, and external approval reference.

Local and linked-dev Chrome/CDP progress in `docs/qa/mvp-ui-qa-progress-report.md` is useful readiness evidence, but it is not a substitute for the external QA artifact package required before production.

## Current External Evidence Target

Recorded 2026-06-15:

- External QA artifact store: Google Drive under `kandacarl@gmail.com`,
  approved by the Diginoces owner on June 15, 2026 under
  `QAART-20260615-OWNER-001`.
- Opaque runbook reference for git/docs: `RBR-GDRIVE-MVP-LAUNCH-001`.
- Artifact-store private-access proof is recorded externally under
  `QAART-20260615-ARTIFACT-ACCESS-001`: Drive metadata reported
  `shared=false` with a single listed owner permission, and an unauthenticated
  export probe returned HTTP 401 with zero protected evidence-marker hits. This
  does not mark any QA scenario row as passed and does not replace folder,
  assigned-tester, retention, encryption, audit-log, or ticketing evidence.
- Staging deployment target: Vercel. Preview build evidence
  `VCL-STAGING-20260615-001` is stored externally after a `READY` deployment.
  Preview environment configuration and a fresh env-backed `READY` deployment
  are stored externally under `VCL-STAGING-20260615-002`. Protected-access
  bypass setup, server-side Supabase secret-key configuration, and app-level
  smoke are stored externally under `VCL-STAGING-20260615-003`; scenario
  evidence capture is in progress.
- Production domain target: `diginoces.com`, with DNS currently managed through
  Bluehost.
- MFA decision: enforce MFA for all sensitive/admin roles before launch.
- Monitoring owner: Carl; backup owner: Diginoces operations; alert channels:
  email and dashboard. Staging monitor signal evidence is recorded under
  `QAART-20260615-QA-024`.
- Rollback owner: Carl; rollback approach approved.

These decisions unblock QA execution planning. Only rows with explicit opaque
evidence IDs below are marked `pass`; each remaining scenario still needs
external artifacts and an opaque evidence ID before production sign-off.

## Status Values

| Status | Meaning |
| --- | --- |
| `pending_external_artifact` | Scenario is defined, but the required external pass/fail evidence package is not recorded here. This blocks production sign-off. |
| `pass` | Scenario passed in the target environment and has an opaque external evidence ID. |
| `fail` | Scenario failed and has a classification, owner, and linked issue/PR. |
| `blocked` | Scenario could not be executed because required access, environment, data, or external artifact infrastructure is missing. |
| `waived` | Scenario is intentionally accepted by the Diginoces owner role and engineering lead with documented remediation. |

## Ledger

| ID | Requirement group | Scenario summary | Current status | Classification | Evidence ID / reference | Owner | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- |
| QA-001 | `PV-*`; `TECH-*` | App loads and health endpoint responds | `pass` | `not_classified` | `QAART-20260615-QA-001` | QA lead | Protected Vercel staging smoke passed for home, health, login, auth redirect, invalid guest token, and unauthenticated protected API denial. |
| QA-002 | `ROLE-*` | Admin signs in and reaches platform routes | `pass` | `not_classified` | `QAART-20260615-QA-002` | QA lead | Protected Vercel staging admin sign-in and route access passed with a disposable fake `diginoces_admin` user: the role required MFA, the AAL1 app callback and `/platform/dashboard` route redirected to `/login/mfa`, the temporary TOTP-verified AAL2 session reached `/platform`, `/platform/projects`, `/platform/dashboard`, `/platform/audit-logs`, and `/platform/reports` with `200` responses, route responses exposed no server-secret markers, and fake role assignment/app-user/auth-user data was cleaned up with zero role/app-user residue. |
| QA-003 | `PROJ-*` | Admin creates a wedding project and events | `pass` | `not_classified` | `QAART-20260615-QA-003` | Operations lead | Protected Vercel staging project/event creation passed after applying the issue #58 code-generation trigger grant migration to linked dev: a disposable AAL2 `diginoces_admin` user created a fake wedding project and civil/reception events through app APIs; project and event codes were generated, project and event API/detail pages returned `200`, `projects.created` and `events.created` audit rows were present, route responses exposed no server-secret markers, fake project/event/app/role data was cleaned up with zero mutable residue, and the disposable auth user was soft-deleted because immutable audit rows intentionally preserve the actor reference. |
| QA-004 | `PAY-*`; `RSVP-*`; `MSG-*` | Contract, pricing, and payment gates are configured | `pass` | `not_classified` | `QAART-20260615-QA-004` | Operations lead | Protected Vercel staging commercial/payment-gate workflow passed with a disposable AAL2 `diginoces_admin` user: fake package/add-on, event package selections, discount, contract generation, contract approval, partial confirmed manual payment, payment exception override, guest-page/invitation gate events, and commercial audit actions were verified; route responses exposed no server-secret markers; mutable fake project/commercial/app/role data was cleaned up with zero residue, and the disposable auth user was soft-deleted to preserve immutable audit actor traceability. |
| QA-005 | `GM-*`; `ROLE-*` | Manual guest creation and update | `pass` | `not_classified` | `QAART-20260615-QA-005` | QA lead | Protected Vercel staging manual guest workflow passed with disposable fake admin, bride, and groom users: admin created a both-side printed-only guest without WhatsApp, bride/groom created and updated own-side guests, an invalid payload was rejected without row creation, bride cross-side create/update returned `403` without mutation, side/event filters behaved correctly, guest audit rows represented creates/updates/event/tag assignments, no route response exposed server-secret markers, fake data was cleaned up with zero mutable residue, and disposable auth users were soft-deleted to preserve immutable audit actor traceability. |
| QA-006 | `GM-*` | CSV guest import mapping and preview | `pass` | `not_classified` | `QAART-20260615-QA-006` | QA lead | Protected Vercel staging CSV import mapping and preview passed with disposable fake admin and bride users: the app APIs created a fake project, event, and existing bride-side guest; bride import history/upload pages returned `200`; a French-header bride-side CSV staged 4 rows, saved mapping, and previewed with `invalidRows=1` and `duplicateWarnings=1`; printed-only without WhatsApp was not blocked, an unknown-event row was blocked, a duplicate existing-guest row warned, oversized CSV upload was rejected by the platform/app size guard, import detail API/pages returned `200`, import audit rows represented created/staged/mapping/validation actions, no route response exposed server-secret markers, fake data was cleaned up with zero mutable residue, and disposable auth users were soft-deleted to preserve immutable audit actor traceability. |
| QA-007 | `GM-*`; `ROLE-*` | Import review and apply | `pass` | `not_classified` | `QAART-20260615-QA-007` | QA lead | Protected Vercel staging import review/apply passed with disposable fake operations-manager and bride users: bride staged and submitted a three-row bride-side CSV; preview returned totalRows=3, validRows=3, invalidRows=0, and duplicateWarnings=0; bride review/apply attempts returned `403`; operations-manager reached AAL2, rendered the review page, approved one row, held one row, rejected one row, and applied approved rows; exactly one guest was created with the expected event assignment, held/rejected rows did not create guests, repeated apply created 0 guests, audit rows represented submit/review/apply/guest creation, no response exposed server-secret markers, fake data was cleaned up with zero mutable residue, and disposable auth users were soft-deleted to preserve immutable audit actor traceability. |
| QA-008 | `PROJ-*`; `GM-*` | Event-specific guest assignment | `pass` | `not_classified` | `QAART-20260615-QA-008` | QA lead | Protected Vercel staging event-specific guest assignment passed with a disposable fake operations-manager user: three events were created, three guests were assigned to reception-only, brunch-only, and both events, API and server-rendered guest-list filters returned the assigned event guests only, the deliberately unassigned event returned zero guests, assignment rows stayed scoped to the disposable project, audit rows represented guest and assignment creation, no response exposed server-secret markers, fake data was cleaned up with zero mutable residue, and the disposable auth user was soft-deleted to preserve immutable audit actor traceability. |
| QA-009 | `RSVP-*`; `ROLE-*` | Public guest page access gate | `pass` | `not_classified` | `QAART-20260615-QA-009` | QA lead | Protected Vercel staging public guest page access gate passed with a disposable fake operations-manager user: two fake guest public-page tokens were created through the app token API; the locked public page returned `200` without exposing fake guest names or event names; the authenticated admin/staff preview returned `200` while the gate was locked and showed only the selected guest; after the disposable project guest-page gate was set to `exception_override` for test setup, token A and token B each returned only their own guest and assigned event, token rows were active guest public-page tokens stored as hashes with `last_used_at` set, audit rows represented token creation, preview, and access actions, no response exposed server-secret markers, fake data was cleaned up with zero mutable residue, and the disposable auth user was soft-deleted to preserve immutable audit actor traceability. |
| QA-010 | `RSVP-*` | Multi-event RSVP | `pass` | `not_classified` | `QAART-20260615-QA-010` | QA lead | Protected Vercel staging multi-event RSVP passed with disposable fake service-side fixture data: a fake guest assigned to four events loaded through the protected public guest page; anonymous token-scoped RSVP submissions saved Yes, No, and Maybe responses; attempted changes from final Yes/No responses returned `locked_final_response`; a Maybe response could be changed to Yes; final RSVP records were one row per assigned event with status counts yes=2, no=1, maybe=1, source `public_guest_page`, open deadline state, and the fake public token reference; audit rows represented token creation, public-page access, RSVP submissions, and one RSVP change; no public page response exposed server-secret markers; and fake project/event/guest/token/RSVP rows were cleaned up with zero mutable residue. |
| QA-011 | `INV-*`; `PDF-*`; `QR-*` | Invitation template registration and field config | `pass` | `not_classified` | `QAART-20260615-QA-011` | Operations lead | Protected Vercel staging invitation template registration and field configuration passed with a disposable fake operations-manager user: invitation list/new-template pages returned `200`; a fake Canva-exported PDF template registration returned `uploaded` with a generated storage path; four dynamic fields were persisted in order for `guest.display_name`, `event.name`, `public_guest_page_qr`, and `public_guest_page_url`; the detail page rendered the configured template and QR field; preview generation transitioned the template to `preview_generated`; approval transitioned it to `technical_preview_approved`; final database state showed preview metadata engine `tested_pdf_worker_abstraction`, field count 4, approval actor, and expected audit actions; no route response exposed server-secret markers; fake project/event/template/user data was cleaned up with zero mutable residue, and the disposable auth user was soft-deleted to preserve immutable audit actor traceability. |
| QA-012 | `INV-*`; `FILE-*` | Invitation generation job foundation | `pass` | `not_classified` | `QAART-20260615-QA-012` | Engineering lead | Traceability: `INV-011`, `INV-012`, `INV-015`, `FILE-005`, `FEAT-INV-004`, `FEAT-INV-006`, `FEAT-INV-009`, `STORY-INV-004`, `TEST-INV-004`, and Sprint 15 release gates `FEAT-REL-005`/`FEAT-REL-010`. Protected Vercel staging invitation generation foundation passed with a disposable fake operations-manager user: an approved fake invitation template and two event-assigned guests produced a queued generation job with `readyCount=2`; the unassigned control guest did not receive a job item; two queued generation job items and two invitation records were created for assigned guests only; authenticated RLS-backed worker-style writes marked one invitation `generated` with dynamic field snapshot engine `tested_pdf_worker_abstraction`; two PDF file versions were stored with SHA-256 checksums, version 1 inactive and version 2 active; the template detail page rendered recent generation job and invitation record information; expected generation, invitation, and file-version audit rows were present; no route response exposed server-secret markers; fake project/event/guest/template/job/invitation/file/user data was cleaned up with zero mutable residue, and the disposable auth user was soft-deleted to preserve immutable audit actor traceability. |
| QA-013 | `MSG-*` | Guided manual WhatsApp send | `pass` | `not_classified` | `QAART-20260615-QA-013` | Operations lead | Traceability: `MSG-001`, `MSG-002`, `MSG-003`, `MSG-004`, `MSG-008`, `MSG-009`, `MSG-010`, `INV-013`, `PAY-014`, `PAY-015`, `FEAT-MSG-001` through `FEAT-MSG-007`, `STORY-MSG-001`, `STORY-MSG-002`, `TEST-MSG-001`, `TEST-MSG-002`, and Sprint 15 release gates `FEAT-REL-005`/`FEAT-REL-010`. Protected Vercel staging guided manual WhatsApp workflow passed with a disposable fake operations-manager user: the communications overview and queue pages returned `200`; active French and English invitation message templates were created and listed; an invitation message prepared through the app API rendered the French template, generated a `wa.me` manual URL from the fake guest WhatsApp number, included the secure public guest-page link, and created no external provider message ID; the detail page rendered `Open WhatsApp`, manual status controls, and `No unofficial automation`; manual `opened_manually` and `sent` status updates returned `200`; database rows showed the message log and queue item synchronized to `sent`, message status events recorded opened then sent, the linked invitation advanced to `sent`, expected message-template/message audit rows were present, no route response exposed server-secret markers, fake project/event/guest/invitation/message/user data was cleaned up with zero mutable residue, and the disposable auth user was soft-deleted to preserve immutable audit actor traceability. |
| QA-014 | `SEAT-*`; `RSVP-*` | Seating assignment and capacity checks | `pass` | `not_classified` | `QAART-20260615-QA-014` | QA lead | Traceability: `SEAT-001`, `SEAT-002`, `SEAT-003`, `SEAT-004`, `SEAT-005`, `SEAT-006`, `SEAT-007`, `SEAT-009`, `RSVP-010`, `FEAT-SEAT-001` through `FEAT-SEAT-006`, `FEAT-SEAT-008`, `FEAT-SEAT-009`, `STORY-SEAT-001`, `STORY-SEAT-002`, `TASK-SEAT-001`, `TEST-SEAT-001`, `TEST-SEAT-002`, `TEST-RSVP-004`, and Sprint 15 release gates `FEAT-REL-005`/`FEAT-REL-010`. Protected Vercel staging seating assignment and capacity workflow passed with a disposable fake operations-manager user: a capacity-1 table accepted three assigned guests with RSVP Yes, Maybe, and No records; seating summary counted assigned guests as 3, active occupancy as 2, over-capacity by 1, and one over-capacity table; RSVP No stayed history-visible while excluded from active occupancy; seating page/map and API returned `200`, expected seating audit rows were present, no response exposed server-secret markers, fake data was cleaned up with zero mutable residue, and the disposable auth user was soft-deleted to preserve immutable audit actor traceability. |
| QA-015 | `SEAT-*`; `FILE-*` | Table-card CSV export | `pending_external_artifact` | `not_classified` | `QAART-pending` | QA lead | Store exported artifact only in approved evidence store. |
| QA-016 | `CHK-*`; `QR-*` | Check-in QR token flow | `pending_external_artifact` | `not_classified` | `QAART-pending` | QA lead | Future check-in token remains separate from guest public token. |
| QA-017 | `CHK-*` | Check-in manual search flow | `pending_external_artifact` | `not_classified` | `QAART-pending` | QA lead | Assigned event staff path required. |
| QA-018 | `CHK-*`; `ROLE-*` | Unexpected guest request review | `pending_external_artifact` | `not_classified` | `QAART-pending` | Operations lead | Include submit and review permission evidence. |
| QA-019 | `WISH-*`; `REP-*` | Public guest wish/message and couple review | `pending_external_artifact` | `not_classified` | `QAART-pending` | QA lead | Include approved export behavior. |
| QA-020 | `REP-*`; `FILE-*` | Reports, exports, audit logs, retention/archive | `pending_external_artifact` | `not_classified` | `QAART-pending` | Engineering lead | PR `#77` redacted audit API snapshot fields and reran a 25/25 authenticated read-positive API matrix; external audit, export, and file lifecycle evidence still required. |
| QA-021 | `PART-*` | Partner profile, submission, and project draft flows | `pending_external_artifact` | `not_classified` | `QAART-pending` | Operations lead | Include partner negative boundaries. |
| QA-022 | `ROLE-*`; `TECH-*` | RLS policy enforcement review | `pass` | `not_classified` | `QAART-20260615-QA-022` | Engineering lead | Linked database lint, dry-run, migration list, advisors, and RPC grant verification were recorded externally; non-allowlisted `PUBLIC`/`anon` execute grant query returned zero rows. |
| QA-023 | `TECH-*`; `FILE-*`; `REP-*` | Security review checklist | `pass` | `not_classified` | `QAART-20260615-QA-023` | Engineering lead | Install, format, lint, typecheck, tests, build, audit, public-env check, and targeted secret scan passed; performance-advisor items remain post-launch follow-up unless staging load evidence escalates them. |
| QA-024 | `REP-*`; `TECH-*` | Monitoring signal validation | `pass` | `not_classified` | `QAART-20260615-QA-024` | Operations lead | GitHub Actions staging health monitor is configured for the protected Vercel Preview `/api/health` endpoint; healthy run `27568966140` passed with two `200`/`ok` samples, and intentional same-origin failure run `27569035612` failed after two `404` samples to prove the dashboard/test-alert path. |
| QA-025 | `ROAD-*`; `TECH-*` | Rollback dry-run | `pending_external_artifact` | `not_classified` | `QAART-pending` | Engineering lead | Include checksum and repository-state evidence. |
| QA-026 | `ROLE-*`; `TECH-*` | Guest token cannot open authenticated app routes | `pass` | `not_classified` | `QAART-20260615-QA-026` | QA lead | Protected Vercel staging authenticated-route denial used a valid fake guest public-page token as a credential header; protected pages returned encoded login redirects, protected APIs returned generic `401`, no response echoed the raw token or protected markers, project/guest audit rows were 0 after route requests, setup-only token audit rows were identified, and temporary fake token rows were cleaned up. |
| QA-027 | `ROLE-*`; `GM-*`; `PAY-*`; `REP-*` | Bride cannot edit groom-only guests or internal/commercial data | `pass` | `not_classified` | `QAART-20260615-QA-027` | QA lead | Protected Vercel staging bride-role negative boundary passed with disposable fake project/users: bride own-side permission true, groom-side false, audit/payment/revenue permissions false; groom-side list and patch, audit read, and payment record requests returned `403`; role-filtered report catalog returned `200` without protected fields; guest/payment/audit mutation counts stayed 0 and fake data was cleaned up. |
| QA-028 | `ROLE-*`; `GM-*`; `PAY-*`; `REP-*` | Groom cannot edit bride-only guests or internal/commercial data | `pass` | `not_classified` | `QAART-20260615-QA-028` | QA lead | Protected Vercel staging groom-role negative boundary passed with disposable fake project/users: groom own-side permission true, bride-side false, audit/payment/revenue permissions false; bride-side list and patch, audit read, and payment record requests returned `403`; role-filtered report catalog returned `200` without protected fields; guest/payment/audit mutation counts stayed 0 and fake data was cleaned up. |
| QA-029 | `ROLE-*`; `PART-*`; `PAY-*`; `REP-*` | Partner cannot access internal/admin project surfaces | `pass` | `not_classified` | `QAART-20260615-QA-029` | QA lead | Protected Vercel staging partner-admin negative boundary passed with disposable fake partner data: the AAL2 fake partner session had own-partner read and partner-dashboard access true while other-partner read, unrelated project read, audit.read, reports.internal.read, and payments.record were false; own-partner read returned `200` as the control; other-partner read/patch, unrelated project read/dashboard, report catalog, internal payment report export, payment record, audit read, and audit export attempts returned `403`; denied responses leaked no fake markers or protected partner/project/payment/audit fields; role assignment, partner user, payment, payment-exception, report-export, and timestamp-bounded audit mutation counts stayed unchanged; fake data was cleaned up. |
| QA-030 | `ROLE-*`; `CHK-*`; `SEAT-*`; `PAY-*`; `REP-*` | Check-in staff cannot access unrelated event or admin workflows | `pass` | `not_classified` | `QAART-20260615-QA-030` | QA lead | Protected Vercel staging event-staff negative boundary passed with disposable fake project/user/events: assigned-event check-in read/perform permissions true, unrelated check-in and seating permissions false, seating table management/global dashboard/audit/payment permissions false; assigned-event check-in overview returned `200` as the control; unrelated check-in, unrelated seating, assigned-event seating admin, commercial, contract, payment, audit, and global dashboard attempts returned `403`; no denied response leaked fake markers or protected fields; check-in/seating/contract/payment/report counts and timestamp-bounded audit mutation rows stayed 0; fake data was cleaned up. |
| QA-031 | `ROLE-*`; `PAY-*`; `TECH-*` | Operations staff cannot perform admin-only functions without grant | `pass` | `not_classified` | `QAART-20260615-QA-031` | Engineering lead | Protected Vercel staging operations-manager negative boundary passed with a disposable AAL2 fake user: operations role was active after temporary TOTP MFA verification with roles.read and payments.record true, while roles.manage and audit.read stayed false; direct Supabase REST role-assignment, permission, and role-definition insert attempts plus audit-log read/export attempts returned `403`; no denied response leaked fake markers or protected fields; role assignment, role definition, permission, and timestamp-bounded audit mutation counts stayed unchanged; fake user was cleaned up. |
| QA-032 | `ROLE-*`; `PROJ-*` | Assigned roles cannot access unassigned projects | `pass` | `not_classified` | `QAART-20260615-QA-032` | QA lead | Protected Vercel staging assigned-role negative boundary passed with disposable fake bride, groom, event-staff, and partner-project-operator users: bride/groom had assigned-project read true and unassigned-project read false, event staff had assigned-event read true and unassigned-project read false, and the partner-project operator reached AAL2 with assigned-project read true and unassigned-project read false; assigned project/event reads returned `200` as controls; unassigned project read and dashboard requests returned `403` for all four actors; denied responses leaked no fake markers or protected project/payment/audit fields; role-assignment and unassigned-project counts stayed unchanged, timestamp-bounded audit mutation rows stayed 0, and fake data was cleaned up with zero QA-032 residue. |
| QA-033 | `ROLE-*`; `TECH-*` | Users cannot self-escalate roles or permissions | `pass` | `not_classified` | `QAART-20260615-QA-033` | Engineering lead | Target Supabase REST self-role-assignment denial passed with a disposable fake authenticated user: `roles.manage=false`, role assignment insert returned `403`, before/after assignments stayed 0, audit mutation rows stayed 0, and the fake user was deleted. |
| QA-034 | `RSVP-*`; `ROLE-*` | Invalid guest tokens are rejected | `pass` | `not_classified` | `QAART-20260615-QA-034` | QA lead | Protected Vercel staging public-page sweep covered expired, revoked, unrelated check-in, and malformed token states; all 4 returned `404`, no protected markers were found, mutation audit rows after the run timestamp were 0, and temporary fake token rows were cleaned up. |
| QA-035 | `TECH-*`; `ROLE-*` | Anonymous callers cannot execute authenticated app RPCs | `pass` | `not_classified` | `QAART-20260615-QA-035` | Engineering lead | Linked target grant verification found 55 authenticated-only RPC signatures and 0 anon/PUBLIC execute violations; anonymous REST sweep returned 404 for all 55 with no protected record fields, and audit rows after the run timestamp were 0. |
| QA-036 | `TECH-*`; `ROLE-*` | Anonymous users cannot execute authenticated API workflows | `pass` | `not_classified` | `QAART-20260615-QA-036` | Engineering lead | Protected Vercel staging sweep covered 66 source-derived authenticated API route/method combinations; all returned generic `401`, and timestamp-bounded audit query returned zero rows. |

## Aggregated Status

| Status | Count |
| --- | ---: |
| `pending_external_artifact` | 8 |
| `pass` | 28 |
| `fail` | 0 |
| `blocked` | 0 |
| `waived` | 0 |

Production decision from this ledger: `no_go` until external evidence is recorded for all 36 scenarios or formally classified with owner approval.

## Update Procedure

1. Run the scenario in the target staging or production-candidate environment using fake data only.
2. Store screenshots, logs, exports, query output, and request/response artifacts in the external encrypted artifact store.
3. Replace `QAART-pending` with an opaque evidence ID or runbook reference, not a direct URL.
4. Set `Current status` to `pass`, `fail`, `blocked`, or `waived`.
5. If status is not `pass`, set a launch classification and link the issue/PR through an opaque reference.
6. Update the aggregated status counts and `docs/planning/mvp-launch-checklist.md`.

